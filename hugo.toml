# qingshuige-theme
#
# This file contains theme-level defaults only. Site identity, content,
# menus, and deployment settings belong to the parent qingshuige-hugo
# project and should not be duplicated here.

[params.qingshuige]
  # Reserved namespace for theme-specific options.
  # Visual defaults will be added after the design direction is agreed.

[module]
  [module.hugoVersion]
    min = '0.158.0'
